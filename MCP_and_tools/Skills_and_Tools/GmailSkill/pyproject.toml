[project]
name = "gmail-skill"
version = "0.1.0"
description = "A skill to manage Gmail operations"
readme = "SKILL.md"
requires-python = ">=3.10"
dependencies = [
    "python-dotenv",
]

[tool.uv]
package = false
